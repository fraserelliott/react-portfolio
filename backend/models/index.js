const { User, Post, Tag, Image, PostTag } = require("./portfolio");
const { Idea, IdeaTag, IdeaTagLink } = require("./ideaspace");

module.exports = {
  User,
  Post,
  Tag,
  Image,
  Idea,
  IdeaTag,
  PostTag,
  IdeaTagLink,
};
