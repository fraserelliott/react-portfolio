{
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm --prefix backend run dev\" \"cross-env VITE_API_URL=http://localhost:3001 npm --prefix frontend run dev\""
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "cross-env": "^10.1.0"
  }
}
